<%= form_with(model: knowledge_area) do |form| %>
  <% if knowledge_area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(knowledge_area.errors.count, "error") %> prohibited this knowledge_area from being saved:</h2>

      <ul>
        <% knowledge_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
